<% provide(:title, "Guest map") %>
<div id="guest">
  <%= render 'users/top'%>
  <div class="title">
    <h1>これはGuest用のページをです。</h1>
    <h3>レビューを投稿するには、ユーザー登録が必要です。</h3>
  </div>
  <div id="google-map">
    <%= react_component("GuestMap", {
      authenticityToken: form_authenticity_token,
      currentUser: 0,
    })  %>
  </div>
</div>
